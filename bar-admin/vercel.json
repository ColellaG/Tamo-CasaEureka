{
  "version": 2,
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist/bar-admin",
  "installCommand": "npm install",
  "framework": "angular",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
} 